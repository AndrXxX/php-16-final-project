{% extends 'baseView.twig' %}

{% block styles %}
  <link href="resources/css/styles.css" rel="stylesheet">
{% endblock %}

{% block body %}


  <div class="container">
    <h2>Лог событий</h2>
      {% if logs is defined %}
        {% for log in logs %}
          <p>{{ log }}</p>
        {% endfor %}
      {% endif %}
  </div>

{% endblock %}